openapi: "3.0.2"
info:
  title: Pizzi API 
  version: "pre-alpha"
  description: >
    This is the internal API documentation for Pizzi.
    You can find out more about Pizzi at [https://www.pizzi-payment.fr](https://www.pizzi-payment.fr).
  contact:
    email: contact@pizzi-payment.fr

servers:
  - url: http://localhost:5000

tags:
  - name: Auth
    description: Everything about authentication.

  - name: Receipts
    description: Everything about receipts querying.

paths:
  /auth/user/register:
    $ref: './endpoints/authentication/register_user.yaml'
  /auth/shop/register:
    $ref: './endpoints/authentication/register_shop.yaml'
  /auth/user/login:
    $ref: './endpoints/authentication/login_user.yaml'
  /auth/shop/login:
    $ref: './endpoints/authentication/login_shop.yaml'
  /auth/disconnect:
    $ref: './endpoints/authentication/disconnect.yaml'

components:
  securitySchemes:
    $ref: './components/security/_index.yaml'

  schemas:
    $ref: './components/schemas/_index.yaml'
