post:
  operationId: "login_shop"
  tags:
    - Auth
  summary: Login Shop
  description: Retreive access token from account credentials.
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../../schemas/requests/login.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../schemas/responses/token.yaml'
    '400':
      description: BAD REQUEST
    '500':
      description: INTERNAL ERROR