post:
  tags:
    - Auth
  summary: Login Shop
  description: Retreive access token from account credentials.
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/login.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/responses/token.yaml'
    '400':
      description: BAD REQUEST
    '500':
      description: INTERNAL ERROR