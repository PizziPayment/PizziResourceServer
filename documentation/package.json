{
  "name": "openapi",
  "version": "1.0.0",
  "description": "Dependencies related to the documentation",
  "scripts": {
    "check": "lint-openapi -vps --ruleset .spectral.yaml openapi.yaml",
    "build": "redoc-cli build openapi.yaml -o index.html"
  },
  "dependencies": {
    "ibm-openapi-validator": "^0.78.0",
    "redoc-cli": "^0.13.14"
  }
}
