get:
  operationId: "shop_list_receipts"
  tags:
    - Shop
  summary: List receipts
  description: List the shop's receipts
  security:
    - BearerAuth: [ ]
  requestBody:
    description: >
      Filters.
      Every field is optional.
    content:
      application/json:
        schema:
          $ref: '../../../schemas/requests/_index.yaml#/ListReceipts'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../../schemas/responses/_index.yaml#/ShopReceiptList'
    '400':
      description: BAD REQUEST
    '401':
      description: UNAUTHORIZED
    '500':
      description: INTERNAL SERVER ERROR
