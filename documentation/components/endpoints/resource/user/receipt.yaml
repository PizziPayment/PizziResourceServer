get:
  operationId: "user_receipt_details"
  tags:
    - User
  summary: Receipt details
  description: Get a receipt's details
  security:
    - BearerAuth: [ ]
  parameters:
    - name: receipt_id
      in: path
      required: true
      description: The receipt's id
      schema:
        type: number
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../../schemas/responses/_index.yaml#/UserReceiptDetails'
    '400':
      description: BAD REQUEST
    '401':
      description: UNAUTHORIZED
    '500':
      description: INTERNAL SERVER ERROR
