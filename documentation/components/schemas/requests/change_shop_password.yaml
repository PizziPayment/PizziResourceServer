type: object
properties:
  password:
    $ref: "../../types.yaml#/password"
  new_password:
    $ref: "../../types.yaml#/password"
  revoke_token:
    type: bool
    description: Remove emitted token(s) for this shop.
example:
  {
    "password": "Old_passw0rd",
    "new_password": "New_passw0rd",
    "revoke_token": true
  }
